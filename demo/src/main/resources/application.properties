# ===============================
# = DATABASE CONFIGURATION
# ===============================
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ASSIGNMENT?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=2210510
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================
# Không tự động tạo schema (bạn có thể chọn update / create-drop nếu muốn)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ===============================
# = JWT CONFIGURATION
# ===============================
app.jwt.secret=super1secret2key3jwt4application520256verystrong
app.jwt.expirationMs=86400000 

# ===============================
# = SERVER CONFIGURATION (Optional)
# ===============================
server.port=8080 
server.error.include-message=always
